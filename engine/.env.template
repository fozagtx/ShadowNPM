# Only OPENROUTER_API_KEY is required — everything else has defaults in code.
# Base URL, payee address, model IDs are all hardcoded in config.ts.

OPENROUTER_API_KEY=sk-or-v1-your-key-here

# Optional overrides:
# SHADOWNPM_PAYEE_ADDRESS=0x...
# SHADOWNPM_TRIAGE_MODEL=...
# SHADOWNPM_INVESTIGATION_MODEL=...
# SHADOWNPM_TEST_GEN_MODEL=...
